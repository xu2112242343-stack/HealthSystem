# 留空：开发时走 Vite 代理（vite.config.ts 将 /api 转到 127.0.0.1:8001）
# 填写完整地址：浏览器直连后端，需后端 CORS 已放行本前端端口
# 改完后请重启 npm run dev
VITE_API_BASE_URL=
# 设为 false 且填写 API 地址后，首页/风险评估会请求 POST /api/risk/predict（需启动 web/backend）
VITE_USE_API_MOCK=true
VITE_APP_NAME=user
